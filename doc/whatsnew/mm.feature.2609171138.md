Added `landloss.io.one_offs.gen_study_extent`, which builds the packaged
`study-areas.geoparquet` from the Territorial Authority 2025 boundaries, and
`get_study_areas`, `get_study_area` and `study_area_bbox` in
`landloss.io.area_of_interest` for reading it. The four territorial authorities
are kept as separate labelled polygons.
