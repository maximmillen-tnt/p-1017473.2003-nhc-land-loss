A new module-level exposure step writes the number of dwellings on every claim
property, and the address-to-claim mapping behind it, so the retaining wall and
crossing sub-caps can be read per dwelling without rebuilding the land extent.
