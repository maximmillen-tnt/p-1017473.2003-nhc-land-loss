Added `landloss.io.readers.get_gwrc_slope_failure` for Greater Wellington's
earthquake-induced slope failure susceptibility zones (Koordinates layer 4069),
and a validation script that plots them by severity with the study area
boundaries and reports how much of each territorial authority the layer actually
maps.
