A new vulnerability step joins every hazard's damage onto one row per property
per realisation, so the chain ends in one table rather than four. It reports the
properties carrying more than one cause, and sizes the double count that would
arise if the Canterbury land damage rates already include retaining wall damage.
