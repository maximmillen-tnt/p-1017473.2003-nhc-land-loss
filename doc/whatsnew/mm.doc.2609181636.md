`hazard/landslide/status.md` now records that ESNZ's probabilistic landslide
model — a 32 m grid of failure probability at discrete shaking levels — is the
starting point, along with the three gaps in it that matter here (no spatial
correlation factor, no smaller landslides, no runout) and the two candidate
routes: build a new model and compare against ESNZ, or extend ESNZ to close the
gaps. Neither route is chosen, so the drafted build-new plan is now presented as
one option rather than as the approach.
