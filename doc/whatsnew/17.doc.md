Added the `maintaining-status-files` skill, covering how each hazard module's
`status.md` is written and kept current: the section order, the brevity rule,
editing in place so the file's git history is a readable record of progress, and
how the weekly progress update to NHC is assembled from these files rather than
written from scratch. Status files now carry an `Updated` date alongside
`Status`.
