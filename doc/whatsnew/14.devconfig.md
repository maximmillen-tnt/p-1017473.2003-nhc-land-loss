`gen_observed_damage_db.py` no longer takes `--losses`/`--obs-dir`/`--lsn-dir`
command-line arguments. The loss GeoPackage is now fetched from T:'s
SourceMaterial through `tdrive_sync.get_source_mat` (caching it locally), and
the National Liquefaction Model release paths are fixed, so there is nothing
left to point elsewhere at.
