The weekly update now reads as a task list rather than a topic list: every plan
item is phrased as an instruction with a leading verb, acronyms are defined once
and then used, and register IDs no longer appear at all — a reader who cannot
open the register has no use for `T-15`, so the update describes the thing
instead. Retaining wall exposure joins the update; culverts is still to come.

`hazard/landslide/status.md` gains a third route, validating or recalibrating the
existing model while keeping it as the primary one, alongside extending it and
building a new one. `hazard/shaking/status.md` records the NSHM scenario route as
parked rather than open, so the demand comes from TS1170.5.

Culvert and bridge exposure joins the update as its own column, and the study
area is now reported as confirmed.
